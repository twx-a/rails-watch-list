<%= simple_form_for [@list, @bookmark] do |f| %>
  <%= f.input :comment, placeholder: "Input a comment" %>
  <%= f.association :movie, prompt: "Choose a movie" %>
  <%= f.submit class: "btn btn-primary" %>
<% end %>
